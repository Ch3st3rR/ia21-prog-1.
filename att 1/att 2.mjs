let x = true;
let nome = []
let oque = []
let eisso = [] 
while (x) {
     console.log("dados:", nome, oque)
    const nome = await r1.question('João') 
    const nome = await r1.question('o que diabos') 
    const nome = await r1.question('é isso?') 
    const falas = await r1.question("continue com as falas")
    
    console.log(eisso.nome)

    eisso.Ah, para.()

    console.log(eisso.nome) 

    eisso.pq, sim.()

}